#include <stdio.h>
#include <stdlib.h>

int main() {
    int n;
    scanf("%d", &n);
    char *p = (char*)malloc(n*sizeof(char));
    gets(p);
    int i, count = 0;
    for(i=0; p[i]!='\0'; i++)
    {
        count++;
    }
    printf("%d",count);
}
